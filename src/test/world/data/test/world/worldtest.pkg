load test.world as worldtest;

storage = motor.World.ArchetypeStorage
(
    componentClass = worldtest.Component;
);

job = worldtest.UpdateKernel
(
    inout = storage.component;
);

world = motor.World.World
(
    products = [job.inout];
);